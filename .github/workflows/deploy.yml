name: Deploy Backend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy
        run: |
          cd ~/perfumebackend

          git fetch origin
          git reset --hard origin/main

          npm ci

          pm2 restart perfumebackend